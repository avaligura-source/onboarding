# NVEX tech — Visual Style Spec

Єдина візуальна система, знята з робочого коду онбордингу NVEX tech.
Передай цей файл цілком в інший чат — його достатньо, щоб адаптувати будь-який сайт під цей стиль.

---

## 1. Суть стилю одним абзацом

Темний технологічний інтерфейс із **лаймовим акцентом на глибокому зеленому**. Два світи: **темна оболонка** (вхід, кіно-моменти, бічна панель) і **світла робоча зона** (контент, тести) — контраст між ними і є головним драматургічним прийомом. Типографіка — щільний геометричний гротеск, заголовки ВЕЛИКИМИ з від'ємним трекінгом, службові підписи — моноширинні з широким розрядженням. Форми стримані, скруглення помірні, тіні м'які й глибокі. Рух — завжди «швидкий старт, довге сповільнення», ніколи не лінійний.

**Ключовий принцип:** лайм — це світло, а не заливка. Він з'являється як акцент, підсвітка, градієнт на кнопці — але ніколи не займає великих площин.

---

## 2. Палітра

### Темна оболонка (основа бренду)

| Роль | HEX | Де застосовується |
|---|---|---|
| Тло сторінки | `#141719` | Основне тло темних екранів |
| Тло поверхонь | `#1B1F1E` | Картки, бічна панель, поля вводу |
| Тло підняте | `#222724` | Ховер-стани, вкладені блоки |
| Тло глибоке | `#0A0E0A` | Повноекранні кіно-моменти (завіси, сплеш) |
| Панель темна | `#111812` | Бічна навігація у світлому шелі |
| Розділювачі | `rgba(255,255,255,.09)` | Усі бордери на темному |

### Акцент — лайм

| Роль | HEX | Примітка |
|---|---|---|
| **Бренд (логотип)** | `#C2DC92` | Головний, з логотипа |
| UI-акцент | `#BBDD88` | Активні стани, іконки, акцентний текст |
| Акцент насичений | `#9CCB62` | Кінець градієнта, натиснуті стани |
| Акцент блідий | `#D3ECA6` | Початок градієнта, підсвітки |
| Акцент на світлому | `#4C8A3F` | Лайм на білому нечитабельний — на світлі лише цей |
| Зелений глибокий | `#08351B` | Тіні, підкладки, бренд-зелений |
| М'який зелений | `#15301E` | Тло акцентних чипів на темному |

**Фірмовий градієнт:**
```css
linear-gradient(93deg, #D3ECA6 0%, #BBDD88 55%, #9CCB62 100%)
```

### Текст на темному

| Роль | HEX |
|---|---|
| Основний | `#F4F6F1` |
| Приглушений | `#A9B0A6` |
| Ледь помітний | `#6C7268` |

### Світла робоча зона

| Роль | HEX |
|---|---|
| Тло зони | `#EAEFDF` |
| Картки | `#FBFCF4` |
| Бордери | `#E3E9D3` |
| Папір (акцентні блоки) | `#E9F1D8` / світліше `#F3F8E8` |
| Лінії на папері | `#D3DFBB` |
| Зелена картка | `#1C3B26` |
| Текст основний | `#152A1B` |
| Текст приглушений | `#41543F` |
| Текст ледь помітний | `#7C8B72` |

### Статуси

Успіх `#10B981` · Помилка `#EF4444`

---

## 3. Типографіка

**Шрифти:** `DM Sans` — весь інтерфейс. `Unbounded` — рідкісні кіно-акценти (субтитри, великі титри). Моноширинний системний стек — службові підписи.

```css
font-family: 'DM Sans','Inter',-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;
--mono: ui-monospace,'SF Mono',SFMono-Regular,Menlo,Consolas,monospace;
```

### Ролі

| Роль | Розмір | Вага | Трекінг | Регістр |
|---|---|---|---|---|
| Hero-заголовок | `clamp(38px,4.6vw,56px)` | 800 | `-.025em` | UPPERCASE |
| Заголовок екрана | 26–34px | 700–800 | `-.02em` | звичайний |
| Підзаголовок | 18–20px | 600 | `-.01em` | звичайний |
| Основний текст | 14–15px | 400–500 | 0 | звичайний |
| Кнопка | 15px | 600 | 0 | звичайний |
| **Eyebrow (службовий підпис)** | 11–12px | 700 | `.1em`–`.18em` | UPPERCASE |
| Мітка поля | 10.5px | 700 | `.09em` | UPPERCASE |

### Два фірмові прийоми

**1. Курсивний лаймовий акцент у заголовку** — одне-два слова виділяються кольором і курсивом:
```css
.t-accent{ color:#BBDD88; font-style:italic }
```
```html
<h1>ВІТАЄМО У <span class="t-accent">NVEX</span></h1>
```

**2. Моноширинний eyebrow** над заголовком — розріджений капс, задає технологічність:
```css
.eyebrow{
  font-family:var(--mono); font-size:11px; font-weight:700;
  letter-spacing:.18em; text-transform:uppercase; color:#BBDD88;
}
/* на світлому тлі: color:#4C8A3F */
```

---

## 4. Форма, простір, глибина

### Скруглення
```
99px — пігулки, чипи, бейджі, прогрес-бари   ← найчастіше
26px — великі картки-слайди
18px — картки, панелі (базове --r)
14px — кнопки
12px / 10px / 8px — поля, дрібні елементи
```

### Тіні (тільки на темному; на світлому — майже пласко)
```css
--shadow-sm: 0 1px 2px rgba(0,0,0,.35), 0 4px 12px rgba(0,0,0,.3);
--shadow-md: 0 2px 6px rgba(0,0,0,.4),  0 12px 32px rgba(0,0,0,.35);
--shadow-lg: 0 8px 24px rgba(0,0,0,.45),0 32px 80px rgba(8,53,27,.5);
```
Зверни увагу: у великій тіні присутній **бренд-зелений** `rgba(8,53,27,.5)` — тіні мають легкий зелений підтон, а не нейтрально-чорні.

На світлій зоні тіні майже відсутні:
```css
box-shadow: 0 1px 2px rgba(50,70,40,.04), 0 10px 30px rgba(50,70,40,.06);
```

### Внутрішні відступи
Картки `30–44px` по вертикалі, `34–56px` по горизонталі. Дрібні блоки `16–20px`. Крок сітки — 4px.

### Фонова текстура
Ледь помітна точкова сітка, розчинена до країв маскою:
```css
.tech-grid{
  position:absolute; inset:0; pointer-events:none;
  background-image:radial-gradient(circle,rgba(255,255,255,.07) 1.2px,transparent 1.2px);
  background-size:24px 24px;
  mask-image:radial-gradient(75% 80% at 50% 50%,#000 25%,transparent 100%);
}
```

---

## 5. Рух

```css
--ease-out:    cubic-bezier(0.23, 1, 0.32, 1);      /* поява, розкриття */
--ease-in-out: cubic-bezier(0.77, 0, 0.175, 1);     /* зникнення, переходи */
--expo-out:    cubic-bezier(0.16, 1, 0.3, 1);       /* кіно-моменти, великі рухи */
--t: 200ms cubic-bezier(0.23, 1, 0.32, 1);          /* дефолт для UI */
```

**Правила:**
- Лінійних кривих не існує. Виняток — нескінченне обертання.
- Мікро-взаємодії 150–250 мс. Переходи екранів 400–700 мс. Кіно-моменти 2–4 с.
- Ховер кнопки: `translateY(-2px)` + підсилення тіні. Натиск: `scale(.98)`.
- Шари в складних сценах **накладаються**, а не йдуть по черзі: кожен наступний стартує до завершення попереднього.
- Обов'язковий блок `@media (prefers-reduced-motion: reduce)` — вимикає анімації, лишає кінцеві стани.

---

## 6. Компоненти

### Головна кнопка
```css
.btn{
  display:inline-flex; align-items:center; justify-content:center; gap:10px;
  padding:16px 32px; border-radius:14px;
  font-size:15px; font-weight:600;
  background:linear-gradient(93deg,#D3ECA6 0%,#BBDD88 55%,#9CCB62 100%);
  color:#10150C;                                   /* темний текст на лаймі */
  box-shadow:0 2px 6px rgba(187,221,136,.2), 0 10px 28px rgba(187,221,136,.16);
  transition:transform var(--t), box-shadow var(--t);
}
.btn:hover{ transform:translateY(-2px) }
.btn:active{ transform:scale(.98) }
```
Другорядна кнопка — прозоре тло, бордер `rgba(255,255,255,.09)`, текст `#F4F6F1`.

### Картка (темна / світла)
```css
/* темна */
.card{ background:#1B1F1E; border:1px solid rgba(255,255,255,.09);
       border-radius:18px; box-shadow:var(--shadow-md) }
/* світла */
.card-light{ background:#FBFCF4; border:1px solid #E3E9D3; border-radius:18px }
```

### Поле вводу
```css
.field input{
  background:#141719; color:#F4F6F1; font-size:14px;
  border:1px solid rgba(255,255,255,.09); border-radius:12px; padding:14px 16px;
  transition:border-color var(--t), box-shadow var(--t);
}
.field input:focus{ border-color:#BBDD88; box-shadow:0 0 0 3px rgba(187,221,136,.15) }
```
Мітка поля — `UPPERCASE`, 10.5px, `letter-spacing:.09em`, колір `#A9B0A6`.

### Чип / бейдж
Пігулка `border-radius:99px`, тло `#15301E`, текст `#BBDD88`, 11px, вага 700, `letter-spacing:.1em`, UPPERCASE.

### Кільцевий прогрес
SVG-коло `r=26`, `stroke-dasharray:163.4`, заповнення через
`stroke-dashoffset: 163.4 * (1 - pct/100)`, колір обведення — лайм, підкладка — `rgba(255,255,255,.09)`.

### Розкладка «оболонка»
Темна бічна панель `300px` (у світлому шелі — `#111812`) + світла робоча зона `#EAEFDF`.
Світлий топбар із напівпрозорим тлом і `backdrop-filter:blur()`.

---

## 7. Що робить стиль впізнаваним

1. **Лайм лише як світло** — акцент, підсвітка, градієнт кнопки. Ніколи не заливає великі площини.
2. **Темне ↔ світле** — різкий контраст оболонки й робочої зони як драматургія, а не випадковість.
3. **Зелений підтон у тінях** — тіні ніколи не нейтрально-чорні.
4. **Моноширинні eyebrow** розрідженим капсом над заголовками.
5. **Курсивний лаймовий акцент** на одному слові в заголовку.
6. **Пігулки `99px`** для всього дрібного — чипи, бейджі, прогрес.
7. **Точкова сітка**, розчинена маскою до країв.
8. **Кіно-моменти на майже чорному** `#0A0E0A` зі світлом, що розквітає з центру.

---

## 8. Чого не робити

- ❌ Лайм великими площинами або як тло під темний текст на всю секцію.
- ❌ Лайм `#BBDD88` як текст на світлому тлі — тільки `#4C8A3F`.
- ❌ Чисто чорний `#000` і чисто білий `#FFF` — усі нейтралі мають зелений підтон.
- ❌ Лінійні анімації та `ease` за замовчуванням.
- ❌ Різкі чорні тіні без зеленого підтону.
- ❌ Синій, фіолетовий, помаранчевий у будь-якій ролі — палітра строго зелено-лаймова.
- ❌ Заокруглення понад `26px` для великих блоків (крім пігулок).
- ❌ Декоративні геометричні фігури інших брендів на фоні.

---

## 9. Готовий блок токенів (копіювати як є)

```css
:root{
  /* акцент */
  --lime:#C2DC92;            /* бренд, логотип */
  --lime-ui:#BBDD88;         /* UI-акцент */
  --lime-deep:#9CCB62;
  --lime-pale:#D3ECA6;
  --lime-on-light:#4C8A3F;   /* лайм на світлому тлі */
  --green-deep:#08351B;
  --green-soft:#15301E;
  --grad:linear-gradient(93deg,#D3ECA6 0%,#BBDD88 55%,#9CCB62 100%);

  /* темна оболонка */
  --bg-deep:#141719;
  --bg:#1B1F1E;
  --bg-soft:#222724;
  --bg-cinema:#0A0E0A;
  --panel-dark:#111812;
  --line:rgba(255,255,255,.09);

  /* текст на темному */
  --ink:#F4F6F1;
  --ink-soft:#A9B0A6;
  --ink-faint:#6C7268;

  /* світла робоча зона */
  --shell:#EAEFDF;
  --card-light:#FBFCF4;
  --line-light:#E3E9D3;
  --paper:#E9F1D8;
  --paper-2:#F3F8E8;
  --paper-line:#D3DFBB;
  --green-card:#1C3B26;
  --inkP:#152A1B;
  --inkP-soft:#41543F;
  --inkP-faint:#7C8B72;

  /* статуси */
  --ok:#10B981;
  --err:#EF4444;

  /* форма */
  --r:18px;
  --r-pill:99px;
  --r-btn:14px;

  /* глибина */
  --shadow-sm:0 1px 2px rgba(0,0,0,.35),0 4px 12px rgba(0,0,0,.3);
  --shadow-md:0 2px 6px rgba(0,0,0,.4),0 12px 32px rgba(0,0,0,.35);
  --shadow-lg:0 8px 24px rgba(0,0,0,.45),0 32px 80px rgba(8,53,27,.5);
  --shadow-light:0 1px 2px rgba(50,70,40,.04),0 10px 30px rgba(50,70,40,.06);

  /* рух */
  --ease-out:cubic-bezier(0.23,1,0.32,1);
  --ease-in-out:cubic-bezier(0.77,0,0.175,1);
  --expo-out:cubic-bezier(0.16,1,0.3,1);
  --t:200ms cubic-bezier(0.23,1,0.32,1);

  /* шрифти */
  --font:'DM Sans','Inter',-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;
  --font-display:'Unbounded','DM Sans',sans-serif;
  --mono:ui-monospace,'SF Mono',SFMono-Regular,Menlo,Consolas,monospace;
}
```

---

## 10. Промпт для іншого чату

> Адаптуй візуал цього сайту під дизайн-систему NVEX tech зі специфікації нижче.
> Збережи структуру й функціональність — зміни лише кольори, типографіку, форми, тіні й анімації.
> Дотримуйся розділу «Чого не робити», встав блок токенів у `:root` і перепиши стилі через ці змінні.
> Шрифти: DM Sans (інтерфейс), Unbounded (кіно-акценти), моноширинний (службові підписи).
>
> [далі вставити весь цей файл]
